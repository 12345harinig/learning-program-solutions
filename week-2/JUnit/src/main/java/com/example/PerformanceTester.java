package com.example;

public class PerformanceTester {

    public void performTask() throws InterruptedException {
        // Simulating a task that takes some time (e.g., DB call, computation)
        Thread.sleep(300);  // 300 milliseconds
    }
}
